[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
testpaths = tests
python_files = test_*.py
addopts =
    --reuse-db
    --cov=apps
    --cov=common
    --cov-report=term-missing
    --strict-markers
markers =
    django_db: mark tests that use the Django database
