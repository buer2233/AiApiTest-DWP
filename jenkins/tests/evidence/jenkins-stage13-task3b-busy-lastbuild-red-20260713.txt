F                                                                        [100%]
================================== FAILURES ===================================
____ test_busy_checks_last_build_api_when_job_payload_omits_building_flag _____

tmp_path = WindowsPath('C:/Users/admin/AppData/Local/Temp/pytest-of-admin/pytest-99/test_busy_checks_last_build_ap0')

    def test_busy_checks_last_build_api_when_job_payload_omits_building_flag(tmp_path):
        _, job_api, _ = job_urls()
        routes = {
            ("GET", job_api): [
                response(200, {"inQueue": False, "lastBuild": {"url": BUILD_URL, "number": 42}})
            ],
            ("GET", f"{BUILD_URL}api/json"): [
                response(200, {"building": True, "result": None, "url": BUILD_URL})
            ],
        }
        client, http, _, _ = make_client(tmp_path, routes)
    
        outcome = client.trigger(build_all=True, run_full_tests=False)
    
>       assert outcome.status == "BUSY"
E       AssertionError: assert 'FAILURE' == 'BUSY'
E         - BUSY
E         + FAILURE

jenkins\tests\test_platform_bootstrap_helper.py:186: AssertionError
=========================== short test summary info ===========================
FAILED jenkins/tests/test_platform_bootstrap_helper.py::test_busy_checks_last_build_api_when_job_payload_omits_building_flag
1 failed in 0.49s
