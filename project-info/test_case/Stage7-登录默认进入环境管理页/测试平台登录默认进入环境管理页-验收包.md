# 测试平台登录默认进入环境管理页-验收包

## 1. 验收结论

- 需求定级：S 档。
- 需求状态：已冻结，主人明确要求登录后默认进入 `/environments`。
- 开发状态：前端路由与登录默认跳转已完成，相关 Playwright、单元测试和构建已通过。
- 验收建议：通过，等待主人终审签字。

## 2. 本阶段交付范围

- 登录页在无显式 `redirect` 参数时默认跳转 `/environments`。
- 平台根路径 `/` 默认 redirect 到 `/environments`。
- 显式 `redirect` 参数仍优先，直接访问 `/dashboard` 等受保护 URL 后登录仍回原目标。
- P1 鉴权路由 Playwright 回归已更新并保留旧的登录、注册、退出、权限校验覆盖。

## 3. 不做事项

- 不删除 `/dashboard` 路由。
- 不新增 UI 页面、导航项或视觉原型。
- 不新增或修改 DRF API。
- 不修改数据库模型、权限模型、Cookie 策略、Jenkins 链路或 Allure 报告入口。
- 不新增 `.env.example` 变量，不提交真实账号、密码、token、Cookie 或生产 URL。

## 4. 关键代码变更

| 模块 | 文件 | 摘要 |
| --- | --- | --- |
| 前端登录 | `../../../front-end/src/views/LoginView.vue` | 新增默认受保护入口常量，登录成功无 `redirect` 时进入 `/environments` |
| 前端路由 | `../../../front-end/src/router/index.ts` | 根路径 `/` 默认 redirect 到 `/environments` |
| 前端 E2E | `../../../front-end/e2e/p1-auth.spec.ts` | 补环境页 mock、默认登录落点、根路径落点、显式 redirect 优先级断言 |

## 5. 验证证据

| 阶段 | 命令 | 证据 | 结论 |
| --- | --- | --- | --- |
| 登录默认落点 RED | `npx playwright test e2e/p1-auth.spec.ts --project=chromium -g "管理人员登录成功后默认进入环境管理页"` | `../../../front-end/tests/evidence/stage7-login-default-environments-red-20260706.txt` | 失败符合预期，实际仍为 `/dashboard` |
| 根路径默认入口 RED | `npx playwright test e2e/p1-auth.spec.ts --project=chromium -g "已登录用户进入平台根路径时默认进入环境管理页"` | `../../../front-end/tests/evidence/stage7-root-default-environments-red-20260706.txt` | 失败符合预期，实际仍为 `/dashboard` |
| 未登录根路径 RED | `npx playwright test e2e/p1-auth.spec.ts --project=chromium -g "未登录用户进入平台根路径时登录目标指向环境管理页"` | `../../../front-end/tests/evidence/stage7-unauth-root-default-environments-red-20260706.txt` | 失败符合预期，旧实现仍为 `/login?redirect=/dashboard` |
| Stage7 目标 GREEN | `npx playwright test e2e/p1-auth.spec.ts --project=chromium -g "(管理人员登录成功后默认进入环境管理页|已登录用户进入平台根路径时默认进入环境管理页|未登录用户进入平台根路径时登录目标指向环境管理页|带受保护 URL 的登录重定向仍优先回到原目标)"` | `../../../front-end/tests/evidence/stage7-default-environments-green-20260706.txt` | 4 passed |
| P1 前端回归 | `npx playwright test e2e/p1-auth.spec.ts --project=chromium` | `../../../front-end/tests/evidence/stage7-p1-auth-playwright-green-20260706.txt` | 21 passed |
| 前端单元 | `npm run test:unit` | `../../../front-end/tests/evidence/stage7-frontend-unit-green-20260706.txt` | 5 files / 9 tests passed |
| 前端构建 | `npm run build` | `../../../front-end/tests/evidence/stage7-frontend-build-green-20260706.txt` | 构建通过；存在既有 warning |

## 6. 截图证据

| 截图 | 路径 |
| --- | --- |
| 登录成功默认进入环境管理页 | `../../../front-end/tests/evidence/screenshots/stage7-login-default-environments.png` |

## 7. 容器化兼容检查

- 本需求只使用前端相对路由 `/environments`，不新增环境变量。
- 未写死 API base URL、Jenkins URL、宿主机端口、本机绝对路径或真实凭据。
- Vite dev server、Playwright baseURL、API 代理仍沿用既有根 `.env` / `.env.example` 机制。

## 8. 独立审查

- 首轮只读 review subagent 发现 1 个 Important：`AC-S7-3.2` 缺少未登录访问 `/` 的直接 Playwright 证据。
- 已补 `未登录用户进入平台根路径时登录目标指向环境管理页` 用例，并完成 RED/GREEN 与 P1 回归。
- 复审结论：Critical 无、Important 无、Minor 无；上一轮 Important 已关闭。
