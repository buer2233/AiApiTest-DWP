# 环境与模块通过率页面-P3-用例详情状态审计与趋势数据-后端最终复审

## 复审说明

- 复审身份：P3 后端最终复审者。
- 复审方式：只读复审；未修改业务代码、测试、迁移、计划文件、git index 或分支状态。
- 写入范围：仅写入本最终复审报告。
- 安全说明：未读取 `.env`，未输出真实凭据。
- 验证范围：仅复核原复审剩余 Important，即 `test_case_result` 当前用例唯一约束是否已改为正式 MySQL 可落库的普通唯一约束方案。
- 测试说明：本轮未重新运行会产生缓存或运行产物的测试命令；证据结论基于已留存 evidence 文件、实现报告和只读代码核对。

### Spec Compliance

✅ Passed

原复审剩余 Important 已闭环。`test_case_result` 已新增 `current_node_key`，当前用例通过模型保存逻辑写入 `node_id`，历史/归档用例以 `is_current=False` 写入 `NULL`；唯一约束已改为普通 `environment + module + current_node_key`，模型和迁移均无 `condition`，符合 MySQL 可落库方案。

### Remaining Important Verification

1. **模型新增 `current_node_key` 且保存规则正确：✅ 通过。**
   `TestCaseResult.current_node_key` 已定义为可空字段（`back-end/metrics/models.py:141`）。`save()` 中按 `is_current` 写入：当前用例保存 `node_id`，历史/归档副本保存 `None`（`back-end/metrics/models.py:178`-`:180`）。

2. **唯一约束为普通 `environment + module + current_node_key`，无 condition：✅ 通过。**
   模型唯一约束字段为 `["environment", "module", "current_node_key"]`，约束名为 `uniq_current_case_result_env_module_node`，未配置 `condition`（`back-end/metrics/models.py:161`-`:168`）。只读扫查未发现 `condition=models` 或 `models.Q(...)` 残留的 partial unique 写法。

3. **迁移与模型一致：✅ 通过。**
   迁移中包含 `current_node_key` 字段（`back-end/metrics/migrations/0002_testcaseresult_casestatusaudit_modulerunhistory_and_more.py:20`），并生成同名普通唯一约束 `environment/module/current_node_key`（`back-end/metrics/migrations/0002_testcaseresult_casestatusaudit_modulerunhistory_and_more.py:39`-`:42`），未包含条件唯一约束。

4. **测试覆盖重复当前用例失败、历史副本允许、约束无 condition：✅ 通过。**
   测试断言当前用例 `current_node_key == node_id`、重复当前用例触发 `IntegrityError`、`is_current=False` 历史副本允许且 `current_node_key is None`（`back-end/tests/test_metrics_case_details_api.py:133`-`:148`）。同时断言约束字段组合为 `("environment", "module", "current_node_key")` 且 `condition is None`（`back-end/tests/test_metrics_case_details_api.py:151`-`:157`）。

5. **证据已更新：✅ 通过。**
   已留存证据显示 `python manage.py check` 通过（`历史验证记录（backend-stage4-p3-manage-check-20260704.txt:1，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`），`makemigrations --check --dry-run` 无变更（`历史验证记录（backend-stage4-p3-makemigrations-check-20260704.txt:1，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`），P3 目标测试为 `40 passed`（`历史验证记录（backend-stage4-p3-target-green-20260704.txt:70，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`），全量回归为 `101 passed`（`历史验证记录（backend-stage4-p3-full-green-20260704.txt:71，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`）。

### Issues

#### Critical (Must Fix)

- 无。

#### Important (Should Fix)

- 无。

#### Minor (Nice to Have)

- 无。

### Assessment

**Task quality:** Approved
**Reasoning:** 原阻塞点已由 MySQL 兼容的可空普通唯一键方案闭环，模型、迁移、测试和留存证据一致。后端可进入前端阶段。
