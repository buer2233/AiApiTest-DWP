# 环境与模块通过率页面-P3-用例详情状态审计与趋势数据-后端独立复审

## 复审说明

- 复审身份：P3 后端独立复审者。
- 复审方式：只读复审；未修改业务代码、测试、迁移、计划文件、git index 或分支状态。
- 复审输入：`.planning/p3-case-detail-audit-trend/backend-review-brief.md`、`.planning/p3-case-detail-audit-trend/backend-implementer-report.md`、`.planning/p3-case-detail-audit-trend/backend-review-diff.txt`、原后端独立审查报告。
- 测试说明：本次按只读要求未重新运行 pytest；测试结论基于实现报告和已留存证据文件。

### Spec Compliance

- ❌ Issues found
- cases/trend 停用环境隔离、错误详情脱敏、状态流转/回滚测试、seed 精确幂等、OpenAPI 错误码描述和证据留存均已补齐；但 `test_case_result` 当前用例唯一性仍未满足正式 MySQL 数据库约束要求。项目正式运行默认使用 MySQL（`back-end/config/settings/base.py:27`-`:55`），测试环境使用 SQLite（`back-end/config/settings/test.py:5`-`:9`）。当前实现使用 `condition=models.Q(is_current=True)` 条件唯一约束（`back-end/metrics/models.py:162`-`:167`；迁移 `back-end/metrics/migrations/0002_testcaseresult_casestatusaudit_modulerunhistory_and_more.py:38`-`:42`），但 Django MySQL 后端声明不支持 partial indexes（`D:\Python3.12\Lib\site-packages\django\db\backends\mysql\features.py:45`），Django schema editor 仅在 `supports_partial_indexes` 为真时创建带 condition 的唯一约束（`D:\Python3.12\Lib\site-packages\django\db\backends\base\schema.py:1767`-`:1777`）。

### Review Fix Verification

1. **cases/trend 对停用环境快照返回 `404 module_snapshot_not_found`：✅ 已修复。**
   cases 查询已增加 `environment__is_active=True` 并在未命中时返回 `module_snapshot_not_found`（`back-end/metrics/views.py:298`-`:304`）；trend 查询同样增加启用环境过滤（`back-end/metrics/views.py:463`-`:469`）。测试覆盖停用环境 cases 404（`back-end/tests/test_metrics_case_details_api.py:111`-`:120`）和 trend 404（`back-end/tests/test_metrics_trend_api.py:76`-`:85`）。

2. **错误详情脱敏覆盖私有/本机 URL、Authorization、password、cookie、token 且有测试：✅ 已修复。**
   脱敏规则已覆盖私有/本机 URL、Bearer Authorization、password、cookie、token 形态（`back-end/metrics/serializers.py:11`-`:25`），管理员详情返回前统一调用脱敏函数（`back-end/metrics/serializers.py:124`-`:127`）。测试数据包含 Authorization、password、Cookie 和私有 URL/token 查询串（`back-end/tests/p3_metrics_helpers.py:114`-`:117`），断言敏感值和私有地址不会出现在管理员详情中（`back-end/tests/test_metrics_case_details_api.py:33`-`:38`）。

3. **`test_case_result` 当前用例数据库唯一约束且不阻断历史归档用例：❌ 未完全修复。**
   SQLite 测试已证明条件唯一约束能阻断重复当前用例、允许 `is_current=False` 历史副本（`back-end/tests/test_metrics_case_details_api.py:132`-`:145`）。但正式数据库是 MySQL（`back-end/config/settings/base.py:27`-`:55`），MySQL 后端不支持 partial indexes（`D:\Python3.12\Lib\site-packages\django\db\backends\mysql\features.py:45`），因此当前 `condition=models.Q(is_current=True)` 唯一约束不会在 MySQL 上形成真实数据库唯一约束。

4. **状态修改测试覆盖三态流转和审计失败事务回滚：✅ 已修复。**
   参数化测试覆盖 `failed -> passed`、`passed -> failed`、`passed -> skipped`、`skipped -> failed`、`skipped -> passed` 的计数刷新（`back-end/tests/test_metrics_case_status_api.py:57`-`:91`）；审计写入失败时断言用例、模块快照、环境快照和审计表均回滚（`back-end/tests/test_metrics_case_status_api.py:94`-`:113`）。实现仍在 `transaction.atomic` 内更新用例、刷新模块/环境统计并写审计（`back-end/metrics/views.py:366`-`:414`）。

5. **seed 精确幂等断言、OpenAPI 业务错误码描述和修复后证据：✅ 已修复。**
   seed 测试两次执行后精确断言当前用例 6 条、趋势 60 条，并验证 distinct 数量一致（`back-end/tests/test_metrics_commands.py:129`-`:142`）。OpenAPI 回归覆盖新增路径、参数以及 `module_snapshot_not_found`、`admin_required`、`case_result_not_found`、`case_status_unchanged`、`archived_case_result`、`invalid_case_status`、`validation_error` 描述（`back-end/tests/test_metrics_swagger_docs.py:37`-`:58`）。实现报告列明 `python manage.py check`、`makemigrations --check --dry-run`、目标测试、actions 回归和完整 pytest 证据（`.planning/p3-case-detail-audit-trend/backend-implementer-report.md:15`-`:32`）；证据文件显示 check 通过（`历史验证记录（backend-stage4-p3-manage-check-20260704.txt:1，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`）、无迁移变更（`历史验证记录（backend-stage4-p3-makemigrations-check-20260704.txt:1，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`）、目标测试 `39 passed`（`历史验证记录（backend-stage4-p3-target-green-20260704.txt:68，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`-`:70`）、actions 回归 `1 passed`（`历史验证记录（backend-stage4-p3-actions-regression-20260704.txt:46，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`-`:48`）、完整 pytest `100 passed` 且总覆盖率 93%（`历史验证记录（backend-stage4-p3-full-green-20260704.txt:69，原本地临时证据已清理；请查询对应历史 Jenkins 构建归档）`-`:71`）。

### Issues

#### Critical (Must Fix)

- 无。

#### Important (Should Fix)

1. **`test_case_result` 当前用例唯一约束在正式 MySQL 上不会落库，原第 3 项阻塞未真正闭环。**
   当前模型和迁移使用条件唯一约束 `condition=models.Q(is_current=True)` 来表达“同一环境/模块/node_id 只能有一条当前用例”（`back-end/metrics/models.py:162`-`:167`；迁移 `back-end/metrics/migrations/0002_testcaseresult_casestatusaudit_modulerunhistory_and_more.py:38`-`:42`）。该方案在 SQLite 测试环境可通过（`back-end/config/settings/test.py:5`-`:9`；`back-end/tests/test_metrics_case_details_api.py:132`-`:145`），但项目正式运行默认 MySQL（`back-end/config/settings/base.py:27`-`:55`），Django MySQL 后端不支持 partial indexes（`D:\Python3.12\Lib\site-packages\django\db\backends\mysql\features.py:45`），schema editor 也会因为不支持 partial indexes 而不创建该唯一约束（`D:\Python3.12\Lib\site-packages\django\db\backends\base\schema.py:1767`-`:1777`）。这会导致正式库仍可能出现重复当前用例，cases 列表和状态修改目标唯一性无法由数据库保证。建议改为 MySQL 可执行的设计，例如引入可空当前唯一键/生成列/归档批次维度等，并增加 MySQL 兼容性验证。

#### Minor (Nice to Have)

- `module_run_history` 唯一约束仍包含可空 `source_run`（`back-end/metrics/models.py:182`-`:203`）。P3 seed 当前传入非空 run，不阻塞本阶段；后续若允许真实同步写入空 `source_run`，MySQL 唯一索引对 `NULL` 组合的行为可能削弱幂等性，建议在 P5 同步协议设计时一并收敛。

### Assessment

**Task quality:** Needs fixes
**Reasoning:** 原 5 项中 4 项已闭环，测试和证据留存充分；但当前用例唯一约束在正式 MySQL 上无法形成真实数据库约束，仍是后端进入前端阶段前必须修复的 Important 问题。
